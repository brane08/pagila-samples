export interface ActorView {
  actorId: number;
  firstName: string;
  lastName: string;
  filmInfo: string;
}

export interface ActorDetail {
  actorId: number;
  firstName: string;
  lastName: string;
  filmInfo: string;
}
